package string;

/**
 * Created by user @ 2/24/14 1:18 PM
 */
public class ShortestAllString {
   /* public String calculate(String[] allWords) {
        List<String> words = new ArrayList<>();
        for (int i = 0; i < allWords.length; i++) {
            for (int j = 0; j < allWords.length; j++) {
                if (i != j && !allWords[i].equals(allWords[j]) && allWords[i].contains(allWords[i])) {
                    break;
                }
            }
        }

        int n = words.size();

    }

    int overlap(String a, String b) {
        int best = 0;
        for (int i = 1; i < Math.min(a.length(), b.length()) + 1; i++) {
            if (b.startsWith())
        }
    }*/
}
